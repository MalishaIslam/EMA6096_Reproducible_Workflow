# services:
#   builder:
#     build:
#       context: .
#       dockerfile: Containerfile
#     working_dir: /app
#     volumes:
#       - .:/app
#     command: make run


services:
  builder:
    build:
      context: .
      dockerfile: Containerfile
    working_dir: /app
    volumes:
      - .:/app
    command: make all
